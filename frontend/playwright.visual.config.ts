import {defineConfig,devices} from '@playwright/test';import path from 'node:path';import {fileURLToPath} from 'node:url';
const frontend=path.dirname(fileURLToPath(import.meta.url));
export default defineConfig({testDir:path.join(frontend,'tests','visual'),testMatch:/design-system\.spec\.ts/,timeout:60_000,fullyParallel:false,workers:1,reporter:'list',expect:{toHaveScreenshot:{maxDiffPixelRatio:0.015,animations:'disabled'}},use:{baseURL:'http://127.0.0.1:5174',locale:'zh-CN',timezoneId:'Asia/Shanghai',viewport:{width:1440,height:900},deviceScaleFactor:1,colorScheme:'light',trace:'retain-on-failure'},projects:[{name:'chromium',use:{...devices['Desktop Chrome'],viewport:{width:1440,height:900},deviceScaleFactor:1}}],webServer:{command:`"${process.execPath}" node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5174`,cwd:frontend,url:'http://127.0.0.1:5174/ui-fixture',timeout:60_000,reuseExistingServer:false}});
