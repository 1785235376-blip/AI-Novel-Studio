// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {AgentResultReview} from './AgentResultReview';afterEach(cleanup);
const job={id:'j1',agent_name:'策划 Agent',status:'COMPLETED',output_schema:'story_plan_proposal',requires_approval:true,result:{structured_output:{summary:'已形成第二幕方案。',proposals:[{}],findings:[]}}};
describe('agent result review',()=>{it('requires an explicit accept or reject action',()=>{const accept=vi.fn(),reject=vi.fn();render(<AgentResultReview job={job} onAccept={accept} onReject={reject}/>);expect(screen.getByText('已形成第二幕方案。')).toBeTruthy();fireEvent.click(screen.getByRole('button',{name:'采纳结果'}));fireEvent.click(screen.getByRole('button',{name:'拒绝结果'}));expect(accept).toHaveBeenCalledWith(job);expect(reject).toHaveBeenCalledWith(job);});it('explains that an accepted result is not automatically applied',()=>{render(<AgentResultReview job={{...job,status:'ACCEPTED',review:{decision:'ACCEPTED',reviewed_by:'author',note:'',applied:false}}} onAccept={vi.fn()} onReject={vi.fn()}/>);expect(screen.getByText(/尚未写入项目/)).toBeTruthy();expect(screen.queryByRole('button',{name:'采纳结果'})).toBeNull();});});
