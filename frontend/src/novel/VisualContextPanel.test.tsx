// @vitest-environment jsdom
import {cleanup,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import {VisualContextPanel} from './VisualContextPanel';import {api} from '../api';
afterEach(cleanup);
describe('VisualContextPanel',()=>{it('shows honest context and controls inclusion',async()=>{vi.spyOn(api,'novel').mockResolvedValue({id:'n1',title:'长夜'} as any);vi.spyOn(api,'resource').mockResolvedValue([] as any);vi.spyOn(api,'chapters').mockResolvedValue([{id:'c1',number:2,title:'雨夜'}] as any);vi.spyOn(api,'assets').mockResolvedValue([] as any);render(<QueryClientProvider client={new QueryClient()}><VisualContextPanel novelId="n1" chapterId="c1"/></QueryClientProvider>);expect(await screen.findByText('长夜')).toBeTruthy();expect(screen.getByText('暂无已关联视觉资产')).toBeTruthy();expect(screen.getByText('只读预览')).toBeTruthy()});it('shows empty project state',()=>render(<QueryClientProvider client={new QueryClient()}><VisualContextPanel/></QueryClientProvider>).getByText('尚未打开小说'))});
