// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {SceneEditor} from './StoryDatabase';afterEach(cleanup);
describe('scene editor',()=>{it('links a scene to volume chapter location and characters',()=>{const onSave=vi.fn();render(<SceneEditor onSave={onSave} volumes={[{id:'fog',title:'迷雾卷'}]} chapters={[{id:'novel:1',title:'第一章'}]} locations={[{id:'mist-port',name:'雾港'}]} characters={[{id:'lin-hai',name:'林海'}]}/>);fireEvent.change(screen.getByLabelText('场景标题'),{target:{value:'抵达雾港'}});fireEvent.change(screen.getByLabelText('所属卷'),{target:{value:'fog'}});fireEvent.change(screen.getByLabelText('所属章节'),{target:{value:'novel:1'}});fireEvent.change(screen.getByLabelText('地点'),{target:{value:'mist-port'}});const people=screen.getByLabelText('出场人物') as HTMLSelectElement;people.options[0].selected=true;fireEvent.change(people);fireEvent.change(screen.getByLabelText('场景目标'),{target:{value:'展示封锁'}});fireEvent.click(screen.getByRole('button',{name:'保存场景'}));expect(onSave).toHaveBeenCalledWith(expect.objectContaining({id:'抵达雾港',volume_id:'fog',chapter_id:'novel:1',location_id:'mist-port',characters:['lin-hai'],purpose:'展示封锁'}));});});
