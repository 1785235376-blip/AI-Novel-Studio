// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {RelationshipEditor} from './StoryDatabase';afterEach(cleanup);
describe('relationship editor',()=>{it('creates a typed relationship between two characters',()=>{const onSave=vi.fn();render(<RelationshipEditor onSave={onSave} characters={[{id:'lin-hai',name:'林海'},{id:'su-ye',name:'苏夜'}]} events={[{id:'port-lockdown',title:'雾港封锁'}]}/>);fireEvent.change(screen.getByLabelText('人物 A'),{target:{value:'lin-hai'}});fireEvent.change(screen.getByLabelText('人物 B'),{target:{value:'su-ye'}});fireEvent.change(screen.getByLabelText('关系类型'),{target:{value:'INTEREST'}});fireEvent.change(screen.getByLabelText('关系开始事件'),{target:{value:'port-lockdown'}});fireEvent.click(screen.getByRole('button',{name:'保存人物关系'}));expect(onSave).toHaveBeenCalledWith(expect.objectContaining({id:'lin-hai-su-ye',source_character_id:'lin-hai',target_character_id:'su-ye',relationship_type:'INTEREST',valid_from_event_id:'port-lockdown'}));});});
