// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {TimelineEditor} from './StoryDatabase';afterEach(cleanup);
describe('timeline editor',()=>{it('creates an event linked to a location, character and chapter',()=>{const onSave=vi.fn();render(<TimelineEditor onSave={onSave} locations={[{id:'mist-port',name:'雾港'}]} characters={[{id:'lin-hai',name:'林海'}]} chapters={[{id:'n:1',title:'第一章'}]}/>);fireEvent.change(screen.getByLabelText('事件标题'),{target:{value:'雾港封锁'}});fireEvent.change(screen.getByLabelText('地点'),{target:{value:'mist-port'}});const people=screen.getByLabelText('关联人物') as HTMLSelectElement;people.options[0].selected=true;fireEvent.change(people);fireEvent.change(screen.getByLabelText('关联章节'),{target:{value:'n:1'}});fireEvent.click(screen.getByRole('button',{name:'保存时间线事件'}));expect(onSave).toHaveBeenCalledWith(expect.objectContaining({id:'雾港封锁',location:'mist-port',characters:['lin-hai'],chapter_id:'n:1'}));});});
