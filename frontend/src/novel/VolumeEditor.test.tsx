// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {VolumeEditor} from './StoryDatabase';afterEach(cleanup);
describe('volume editor',()=>{it('captures ordered chapter range and goal',()=>{const onSave=vi.fn();render(<VolumeEditor onSave={onSave}/>);fireEvent.change(screen.getByLabelText('卷标题'),{target:{value:'迷雾卷'}});fireEvent.change(screen.getByLabelText('本卷目标'),{target:{value:'建立悬念'}});fireEvent.change(screen.getByLabelText('起始章节'),{target:{value:'1'}});fireEvent.change(screen.getByLabelText('结束章节'),{target:{value:'8'}});fireEvent.change(screen.getByLabelText('状态'),{target:{value:'WRITING'}});fireEvent.click(screen.getByRole('button',{name:'保存卷规划'}));expect(onSave).toHaveBeenCalledWith(expect.objectContaining({id:'迷雾卷',title:'迷雾卷',sequence:1,goal:'建立悬念',start_chapter:1,end_chapter:8,status:'WRITING'}));});});
