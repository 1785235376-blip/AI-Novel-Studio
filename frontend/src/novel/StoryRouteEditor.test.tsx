// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {StoryRouteEditor} from './StoryDatabase';afterEach(cleanup);
describe('story route editor',()=>{it('creates a child ending from an original route',()=>{const onSave=vi.fn();render(<StoryRouteEditor onSave={onSave} routes={[{id:'original',title:'原剧情'}]}/>);fireEvent.change(screen.getByLabelText('路线标题'),{target:{value:'结局 A'}});fireEvent.change(screen.getByLabelText('路线类型'),{target:{value:'ENDING_A'}});fireEvent.change(screen.getByLabelText('父路线'),{target:{value:'original'}});fireEvent.change(screen.getByLabelText('分歧章节'),{target:{value:'12'}});fireEvent.change(screen.getByLabelText('共享基线截止章节'),{target:{value:'11'}});fireEvent.click(screen.getByRole('button',{name:'保存剧情路线'}));expect(onSave).toHaveBeenCalledWith(expect.objectContaining({id:'结局 A',route_type:'ENDING_A',parent_route_id:'original',divergence_chapter:12,shared_until_chapter:11}));});});
