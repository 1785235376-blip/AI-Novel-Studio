// @vitest-environment jsdom
import {cleanup,fireEvent,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {StoryPlanningWorkspace} from './StoryPlanningWorkspace';afterEach(cleanup);
describe('StoryPlanningWorkspace',()=>{it('shows persisted hierarchy without invented data',()=>{const onOpen=vi.fn(),onOpenChapter=vi.fn();render(<StoryPlanningWorkspace novelTitle="长夜" outline={{premise:'寻找失踪的城',main_conflict:'时间耗尽'}} volumes={[{id:'v1',title:'启程',start_chapter:1,end_chapter:2}]} chapters={[{id:'c1',title:'雨夜',number:1,word_count:1200}]} scenes={[{id:'s1',title:'车站',chapter_id:'c1',sequence:1,purpose:'拿到车票',conflict:'封锁'}]} onOpen={onOpen} onOpenChapter={onOpenChapter}/>);expect(screen.getByText('拿到车票')).toBeTruthy();expect(screen.getAllByText('未记录').length).toBeGreaterThan(0);expect(screen.getByText('拖拽即可排序')).toBeTruthy();expect(screen.queryByText(/示例/)).toBeNull();fireEvent.click(screen.getByText(/第 1 章/));expect(onOpenChapter).toHaveBeenCalledWith('c1');fireEvent.click(screen.getByText(/1\. 车站/));expect(onOpen).toHaveBeenCalledWith('scenes','s1')});it('shows loading, error and empty states',()=>{const p={novelTitle:'空白',onOpen:vi.fn(),onOpenChapter:vi.fn()};const {rerender}=render(<StoryPlanningWorkspace {...p} loading/>);expect(screen.getByRole('status').textContent).toContain('正在读取');rerender(<StoryPlanningWorkspace {...p} error="读取失败"/>);expect(screen.getByText('读取失败')).toBeTruthy();rerender(<StoryPlanningWorkspace {...p}/>);expect(screen.getByText('还没有故事规划')).toBeTruthy()})});
