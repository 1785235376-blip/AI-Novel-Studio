// @vitest-environment jsdom
import {cleanup,render,screen} from '@testing-library/react';import {afterEach,describe,expect,it,vi} from 'vitest';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import {CapabilityStatusCenter} from './CapabilityStatusCenter';import {api} from '../api';afterEach(cleanup);
describe('CapabilityStatusCenter',()=>{it('reports real unavailable video capability',async()=>{vi.spyOn(api,'multimodalHealth').mockResolvedValue({});vi.spyOn(api,'videoProviders').mockResolvedValue({items:[]});vi.spyOn(api,'pluginRuntimeStatus').mockResolvedValue({execution_supported:false,sandbox:'none'});vi.spyOn(api,'workflows').mockResolvedValue({items:[]});render(<QueryClientProvider client={new QueryClient()}><CapabilityStatusCenter module="VIDEO" novelId="n1"/></QueryClientProvider>);expect(await screen.findByText('未配置')).toBeTruthy();expect(screen.getByText('未实现')).toBeTruthy()})});
