# V0.2 Runtime + V0.3 Writer UI Completion Report

## 交付统计

- 修改现有文件：8（项目未初始化 Git，按本次审计基线清单统计）
- 新增文件：31（总文件数由 90 增至 121）
- 删除文件：0
- 新增目录：`frontend/`、`frontend/src/`

## Backend API

Health/Providers/Models；Novel CRUD；Chapter list/create/read/update；Characters/Locations/Canon/Foreshadowing/Timeline/安全 Secrets；Continue/Rewrite/Polish/Brainstorm/Review；Generation status/SSE/cancel/accept/reject；Context Preview；Pending Canon list/approve/reject。V0.1 `/health`、`/novels`、`/context-packs` 保留。

## Frontend 页面与区域

Novel Selection、Writer Workspace、Novel/Chapter Tree、TipTap Chapter Editor、AI Workspace、Draft Diff Preview、Context/Canon/Secrets Inspector、Pending Canon Review 组件、Runtime Status Bar。

## Runtime / Provider

- IMPLEMENTED：PostgreSQL+backend+frontend Compose 定义；相对路径；环境变量配置。
- IMPLEMENTED / STATIC VERIFIED：Ollama Provider 同步/流式、模型枚举；OpenAI-compatible；Mock Provider；Cloud→Local/Mock 路由。
- MOCK VERIFIED：Mock Streaming、Cancel 协作逻辑、Draft Accept 闭环。
- PARTIAL / NOT VERIFIED：Docker、PostgreSQL 容器、Ollama、OpenAI/DeepSeek/OpenRouter 实际请求。
- NOT IMPLEMENTED：Anthropic/Gemini 原生协议（只返回配置/状态）。

## Tests

Python AST PASS；PowerShell parse PASS；Mock Draft→Accept→Pending PASS；前端结构 PASS。FastAPI/pytest/npm/Vitest 依赖未安装，因此真实 API 和 UI build NOT VERIFIED。

## 未完成 / PARTIAL

章节 Rename/Delete/Move/Duplicate 的完整 UI；Novel Import/Export UI；Pending Canon Edit+Approve 与导航入口；段落级 Diff；PostgreSQL Repository 取代当前文件 Runtime；结构化 JSON 日志落盘；成本 Guard；原生 Anthropic/Gemini；真实 Dify。

## 已知 Bug / 风险

- UI 的 Markdown→TipTap 转换是基础段落转换，复杂 Markdown 可能丢失。
- Rewrite 使用浏览器 Selection，未完全绑定 TipTap selection。
- Auto-save 与快速切换章节之间尚无冲突版本检查。
- Job 为进程内存状态，后端重启会丢失未接受 Draft；数据库表已预留但尚未接 Repository。

## V0.1 Bug 修复

V0.1 Workflow 会在 AI 输出后立即保存章节，不符合 V0.3 Draft 安全边界。新 Generation Pipeline 不调用该直接保存路径：生成仅保存在 Job，用户 Accept 后原子提交。旧 Workflow 保留兼容，无 Breaking Change。

## 下一阶段建议

先安装开发依赖并完成 ASGI/Vitest/TypeScript build；随后接 PostgreSQL Repository 与持久 Job queue，再完善 TipTap Selection、章节版本冲突和 Import/Export UI。最后做真实 Ollama 与单一 Cloud Provider 验收。
