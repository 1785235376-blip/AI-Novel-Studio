# Development

复制 `.env.example` 为 `.env`。无模型开发设置 `MOCK_PROVIDER=true`。安装 Python 3.11+/Node 20+ 后分别安装根目录与 frontend 依赖，运行 `scripts/dev.ps1`。Docker 路径运行 `scripts/start.ps1`，前端为 3000，API 为 8000。脚本不会安装 Docker/Ollama/模型。
