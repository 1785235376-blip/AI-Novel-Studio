# V0.4 Production Stabilization Report

## IMPLEMENTED

- TipTap JSON Document 为内部正文模型，Markdown 保持开放导出；旧 Markdown 按需迁移。
- Chapter version、乐观锁、历史快照、Restore、AI/USER source 字段。
- Chapter Delete/Duplicate API；History API。
- Generation Job 文件持久 Repository，启动时将未完成任务恢复为明确 FAILED，避免假装仍在运行。
- Repository 分层骨架：Novel/Chapter/Canon/Generation。
- `003_document_versions.sql`，没有修改既有 migration。
- JSONL 白名单结构化日志，拒绝 prompt/key/secret 字段。
- TipTap Selection API 组件支持 `{from,to,text}`。
- `/health` 前端 Dashboard 与更清晰的开发启动检测。
- 修复 setuptools 多顶层包误发现问题，无 Breaking Change。

## REAL VERIFIED

- Python 项目依赖安装成功。
- FastAPI 0.141.1 真实导入成功。
- TestClient：`GET /api/health` 与 `GET /api/novels` 返回 200。
- TypeScript 5.9.3 `tsc --noEmit` 通过。

## MOCK / LOCAL VERIFIED

- Markdown→Document→Markdown round-trip。
- Version increment、冲突拒绝、History Restore。
- Generation Repository 跨实例读取。
- 既有 Mock Draft→Accept→Pending Canon 闭环。
- pytest 选定 8 项断言全部通过，但 runner 在结果输出后未退出，被 20 秒外层超时终止；不能标完整 REAL VERIFIED。

## PARTIAL / NOT VERIFIED

- Vite/Vitest 因当前沙箱拒绝在 `frontend/node_modules/.vite-temp` 写临时文件（EPERM）未完成；不是 TypeScript 错误。
- Docker、PostgreSQL Compose、Ollama、Cloud Provider 未安装/未配置，NOT VERIFIED。
- Job PostgreSQL Queue Schema 已有，但运行时使用文件持久 Repository；真正多进程队列 NOT IMPLEMENTED。
- TipTap Selection 组件已实现，但现有单行 App 尚未把 selection state 传入 Rewrite 请求，PARTIAL。
- Chapter Rename/Move、Import/Export UI、Pending Edit+Approve、段落级 Diff UI 尚未完成。

## KNOWN ISSUES

- 未接受 Job 可恢复查看，但启动时统一标记 FAILED，不会自动重新执行，以避免重复扣费/重复写作。
- 文件 Repository 适合单用户本机；多进程部署必须切 PostgreSQL Repository。
- 自动保存 API 支持 version conflict，但当前 UI 尚未呈现 Keep Local/Keep Server/Compare 对话面板。
