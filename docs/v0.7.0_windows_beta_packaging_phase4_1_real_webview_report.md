# V0.7.0 Windows Beta Packaging Phase 4.1 - Real WebView2 Verification

## Status

PASS. The unchanged authorized C#/.NET Windows Forms host was restored, compiled, and executed against WebView2 Evergreen. Two bounded real host launches completed the Phase 3 one-time bootstrap, activated an opaque packaged session, and reached an existing protected collaboration path through the trusted session resolver. No installer, updater, self-contained runtime assembly, database, provider, generation, authorization, design-system, schema, or migration work was performed.

## Build environment

- Build-machine SDK: .NET SDK 8.0.424, installed under the operator cache only. It is not an end-user runtime prerequisite.
- Existing machine runtimes: .NET runtime present; WebView2 Evergreen 151.0.4129.78 present.
- Declared and restored package: Microsoft.Web.WebView2 1.0.3537.50.
- Restore: PASS from the declared project dependency.
- Release build: PASS, zero errors and one WindowsBase assembly-version resolution warning.
- Output: `desktop-host/AI.NovelStudio.DesktopHost/bin/Release/net8.0-windows/AI-Novel-Studio.DesktopHost.exe`.

## Real host evidence

- Actual compiled host executed: YES, twice.
- Visible Win32 host window: YES on both launches.
- WebView2 initialization and packaged origin navigation: PASS on both launches.
- `DESKTOP_SESSION_READY`: observed on both launches only after bootstrap and protected probe completion.
- Protected request: `GET /api/collaboration/admin/workspaces` returned 200 through the opaque packaged session and server-owned Trusted ActorContext.
- Bootstrap use count: exactly one successful exchange per runtime; replay returned 401.
- Restart: second launch used a different runtime instance ID.
- Old session after invalidation: 401.
- Old bootstrap against the later runtime: rejected; no exchange occurred.
- Controlled close: WM_CLOSE completed with host exit code 0 on both launches.
- Host process leaks attributable to the run: 0.
- WebView profile marker remained after close on both launches; UserData deletion: 0.

## Storage and security

- Packaged localStorage entries: 0.
- Packaged sessionStorage entries: 0.
- Packaged IndexedDB databases: 0.
- Development-session fallback: 0.
- URL token exposure: 0.
- Public listeners: 0; both verification HTTP listeners were bound to 127.0.0.1 and stopped by the bounded harness.
- Bootstrap/session/ownership/DeepSeek secret values were not written to the report, URLs, runtime metadata, or host output.

## Tests

- Phase 1 packaging, Phase 2 ownership, Phase 3 bootstrap/security, and Phase 4 desktop bridge/host contract: 49 passed, 2 warnings.
- TypeScript `--noEmit`: PASS.
- Full frontend Vitest: BLOCKED BY ENVIRONMENT. Vite cannot create `frontend/vite.config.ts.timestamp-*.mjs` (`EPERM`, open).
- Frontend production build: BLOCKED BY ENVIRONMENT. TypeScript cannot rewrite `frontend/tsconfig.tsbuildinfo` (`TS5033`, `EPERM`, open).
- The frontend directory is owned by the sandbox account while existing generated subtrees/files have different ownership. The failure predates Phase 4.1. No broad ACL or ownership changes were attempted.
- Backend aggregate: NOT RERUN. The frozen nine-failure collaboration-setting isolation debt remains classified as pre-existing.

## Freeze checks

- Database change: NO.
- Schema: NONE.
- Migration: NONE.
- Provider changes: 0.
- Generation changes: 0.
- Authorization model changes: 0.
- DS changes: 0.
- Installer/runtime assembly/updater: NOT STARTED.

## Next recommended phase

V0.7.0 Windows Beta Packaging Phase 4.2 - Frontend ACL Isolation Closure ONLY. Establish a repo-owned, run-scoped writable frontend verification copy or obtain narrowly scoped ACL repair approval, then rerun full Vitest and production build without changing product behavior. Do not begin installer implementation until that isolated environment gate is green.
