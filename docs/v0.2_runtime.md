# V0.2 Runtime

后端在 PostgreSQL 不可用时使用开放文件运行；数据库容器可用后提供结构化事实持久层。Ollama 为宿主服务，支持状态、模型枚举、同步及 NDJSON 流式生成。Mock Provider 支持无模型开发。所有生成经 AgentRunner→ModelRouter→Provider，SSE 暴露增量文本；取消通过协作式 Event 控制。Generation Draft 仅 Accept 后原子提交。
