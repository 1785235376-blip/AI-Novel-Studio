# V0.5.3 Phase 1F.1 Parity Isolation Closure

## Result

The previous Timeline mismatch was test-data contamination, not a repository
semantic mismatch. The parity harness now creates a unique run namespace and a
dedicated PostgreSQL novel/project for every execution. Evidence and all domain
fixture IDs are bound to that run ID; historical validation rows are retained
and cannot enter the current comparison.

## Verification

Three consecutive real File/PostgreSQL runs returned:

```text
Run 1: MATCH
Run 2: MATCH
Run 3: MATCH
```

Timeline, Location, Relationship, Canon Dependency, Character Knowledge, and
Continuity Finding all matched, including evidence lookup and idempotent create.

The full regression suite remains `72 passed, 9 skipped`.

## Decision

Repository parity is now `MATCH` for the isolated fixture. This report does not
claim Phase 1 PASS by itself; rule, service, API-visible, and failure-isolation
gates must be recorded together in the final validation run.
