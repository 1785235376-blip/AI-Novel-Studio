# AI-Novel-Studio V0.6 产品化进度

更新时间：2026-08-24  
当前阶段：V0.6 收尾验收（完成）

## 进度总览

| 阶段 | 状态 | 说明 |
|---|---|---|
| 真实代码/API 扫描 | 完成 | 已确认 AppShell、Feature ID、Context/Lore/Story DB/Agent Service、DesktopHost 打包链路 |
| P0 左侧功能浮层 | 完成 | 章节独立滚动、底部启动器、向上浮层、焦点/键盘/记忆 |
| P0 自动化验证 | 完成 | Vitest、lint、build、Impeccable detector |
| P0 正式 DesktopHost | 完成 | packaged launcher、标准 local verifier、尺寸/DPI/视觉证据 |
| Phase 1 AI Context Preview | 完成（代码、测试、正式 DesktopHost） | Inspector 内显式刷新，真实 Agent Context + 写作目标/Canon/批准规则状态；已完成 packaged launcher 回归 |
| Phase 2 Generation Workflow | 完成（代码、测试、正式 DesktopHost 运行时） | 真实 Job/前端流程状态映射为 Context → Plan → Generation → Review 时间线；交互态视觉矩阵待真实 Provider 环境补证 |
| Phase 3 World Building Dashboard | 完成（代码、测试、正式 DesktopHost） | 真实角色关系、时间线与伏笔总览；packaged runtime verifier 已通过 |
| Phase 4 Story Planning Workspace | 完成（代码、测试、正式 DesktopHost） | 真实大纲→卷→章节→场景层级；本机相邻章节拖拽复用持久化 move API，失败刷新回滚 |
| Phase 5 Visual Context | 完成（代码、测试、正式 DesktopHost） | 图片工作区读取真实小说视觉上下文，生成前可选择纳入提示词 |
| Phase 6 非小说模块状态中心 | 完成（代码、测试、正式 DesktopHost） | 图片、视频、声音、资产、插件、Workflow 统一显示真实能力状态 |
| Phase 7 Agent Activity Center | 完成（代码、测试、正式 DesktopHost） | 真实 Agent Job 活动、状态、输入输出摘要、错误与重试关系 |
| V0.6 收尾验收 | 完成 | 前端全量回归、后端定向回归、正式 DesktopHost verifier |
| Phase 8+ | Deferred | 不在本轮扩张底层能力 |

## P0 交付

实现文件：

- `frontend/src/ui/FeatureLauncher.tsx`
- `frontend/src/ui/FeatureLauncher.css`
- `frontend/src/ui/FeatureLauncher.test.tsx`
- `frontend/src/ui/FeatureLauncher.css.test.ts`
- `frontend/tests/e2e/p0-feature-launcher.spec.ts`
- `frontend/src/ui/AppShell.tsx`（增加可选 sidebar class）
- `frontend/src/ui/primitives.tsx`（Tooltip/Button 可访问性最小修正）

既有 feature ID、业务回调和 `studio-feature-groups` localStorage key 未改变。

## Phase 1 交付

实现文件：

- `frontend/src/novel/AiContextPreviewPanel.tsx`
- `frontend/src/novel/AiContextPreview.css`
- `frontend/src/novel/AiContextPreviewPanel.test.tsx`
- `frontend/src/novel/AiWritingPanel.tsx`（传入当前小说/章节/默认目标并挂载预览）
- `frontend/src/api.ts`（补齐既有 Agent Context 的 instruction 参数与类型；扩展既有 generic context 参数，不改后端路由）
- `frontend/src/App.tsx`（传入当前章节和 local/cloud 模式）

### 数据边界

主预览使用后端已有 `AgentContextService` 端点，因而能显示实际 `chapter_id`、`chapter_version`、`context_hash`、角色范围、`writing_context`、source manifest 和云端隐私省略。写作目标、Canon、已批准世界规则通过现有 Service API 读取。所有来源都在真实请求完成后才显示“已读取”；失败、空值、权限和关闭的可选能力分别显示真实状态。

### 运行时开关

没有新增 Feature Flag。Lore、Narrative、Context Pack V2 的显示沿用后端现有 `ENABLE_LORE_CONTEXT`、`ENABLE_NARRATIVE_CONTEXT`、`ENABLE_CONTEXT_PACK_V2`；开关关闭时面板显示“未配置”，不会补入示例内容。

## 验证命令与结果

```powershell
cd D:\小说\AI-Novel-Studio\frontend
npm test
npm run lint
npm run build

cd D:\小说\AI-Novel-Studio
.venv\Scripts\python.exe -m pytest tests/test_phase6_agent_context.py -q
```

当前结果：前端测试 45 files/165 tests、token lint、production build、Impeccable detector 通过；后端 Agent Context 定向测试 3 passed，Lore/Context 合同回归 5 passed/2 skipped（real PostgreSQL 不可用）。全量 pytest 在执行窗口内未完成，已知既有回归见报告。

## Phase 2 交付（代码与自动化）

实现文件：

- `frontend/src/novel/GenerationWorkflowTimeline.tsx`
- `frontend/src/novel/GenerationWorkflowTimeline.css`
- `frontend/src/novel/GenerationWorkflowTimeline.test.tsx`
- `frontend/src/novel/AiWritingPanel.tsx`（挂载时间线并复用现有 Review）
- `frontend/src/App.tsx`（传递恢复状态，不改生成 API）

时间线不新增后端阶段或定时模拟。请求提交期间只显示“读取创作上下文”进行中；后端成功返回 `job_id` 后，才将上下文接收和可追踪任务计划标为完成；草稿阶段由真实 `GENERATING / COMPLETED / FAILED / CANCELLED` 或多候选轮询结果驱动；Review 由现有草稿 ready、accept/reject 前端动作驱动。恢复应用时继续使用既有 `generationRecovery` 元数据和 `/api/generation/{id}` 查询，并显式显示“已恢复任务”。

失败阶段展示真实错误。单草稿复用既有审核区重试，避免重复操作；多方案在时间线中列出失败候选的定向重试，同时保留成功候选审核。采用仍调用既有 accept API、版本校验、章节刷新和冲突流程；接受前不会覆盖正文。

Phase 2 最终包：

- package/evidence root：`D:\小说\.desktop-v1-acceptance-p2-20260824-r12`
- ASCII launcher staging：`D:\AI-Novel-Studio-P2-Application-r12`
- verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`
- 限制：本轮没有向验收环境注入真实 Provider 凭据，因此运行/失败/Review/恢复各状态的 DesktopHost 视觉矩阵未补证，不以静态或模拟任务代替。

P0 正式 DesktopHost：

- package/evidence root：`D:\小说\.desktop-v1-acceptance-p0-20260824-r9`
- verifier：`application_ready=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`
- 证据：1280×720、1440×900、1920×1080、120 DPI，以及 launcher scroll/overlay/selection/Escape/focus

Phase 1 最终包：

- package/evidence root：`D:\小说\.desktop-v1-acceptance-p1-20260824-r10`
- ASCII launcher staging：`D:\AI-Novel-Studio-P1-Application-r10`
- verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`
- 证据：真实小说工作区、1920×1080、120 DPI；入口与工作区 PNG 已随包保存

## 阻塞与下一步

1. 当前 Playwright P0 E2E 因本机 PostgreSQL `127.0.0.1:5432` 未启动而保持 blocked；不以浏览器开发页替代 DesktopHost 验收。
2. Phase 1 已完成正式 DesktopHost 回归；后续只需在有 PostgreSQL 的 CI/验收机补跑被阻塞的 Playwright E2E 与全量后端回归。
3. Phase 2 正式 DesktopHost 运行时已通过；真实 Provider 交互态视觉矩阵作为补证项保留。

## Phase 3 交付（代码与自动化）

实现文件：

- `frontend/src/novel/WorldBuildingDashboard.tsx`
- `frontend/src/novel/WorldBuildingDashboard.css`
- `frontend/src/novel/WorldBuildingDashboard.test.tsx`
- `frontend/src/App.tsx`（复用 Story Database 既有 queries，并连接既有编辑分类）

数据直接来自现有 `characters`、`relationships`、`timeline`、`foreshadowing`、`locations` Service/API；没有新增数据库、路由、Provider 或示例数据。角色关系显示双方角色、关系类型、说明、起始事件与当前状态；时间线按真实 `sequence` 排序，显示故事时间、地点、人物、章节和冲突状态；伏笔显示首次章节、目标章节、关联人物、关联事件、当前状态和是否解决。

伏笔数据合同没有“每次后续出现章节”字段，因此界面只显示真实关联事件，不把 `target_chapter` 描述为实际出现位置。任一视图可跳转到既有编辑分类和对应记录；loading、empty、单来源 error 均有独立状态。

验证结果：前端 46 files / 169 tests，lint、production build、Impeccable detector 通过；后端 `test_phase4_relationships.py`、`test_phase4_timeline.py`、`test_phase4_foreshadowing.py` 共 3 passed。

Phase 3 最终包：

- package/evidence root：`D:\小说\.desktop-v1-acceptance-p3-20260824-r13`
- ASCII launcher staging：`D:\AI-Novel-Studio-P3-Application-r13`
- verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`
- DesktopHost DLL provenance hash：`07069966D8B3064DC984BDD53FF1D660B4A75CE3800752044463F0519A130873`

下一阶段：Phase 4 Story Planning Workspace。先确认现有章节排序 API；没有真实排序接口时保持 Deferred，不在前端伪造持久化拖拽。

## Phase 4 交付（代码、自动化与正式 DesktopHost）

实现文件：

- `frontend/src/novel/StoryPlanningWorkspace.tsx`
- `frontend/src/novel/StoryPlanningWorkspace.css`
- `frontend/src/novel/StoryPlanningWorkspace.test.tsx`
- `frontend/src/novel/ChapterTree.tsx`、`ChapterTree.test.tsx`、`novel.css`
- `frontend/src/App.tsx`

规划工作区复用现有 outline、volumes、chapters、scenes、timeline、foreshadowing 查询，按真实卷章节范围和 scene `chapter_id` 形成 Novel → Outline → Volume → Chapter → Scene 层级。章节目标与冲突来自已保存场景，关键事件和伏笔来自已有记录；当前合同没有章节级人物变化聚合时明确显示“未记录”，不补示例数据。

本机章节拖拽调用既有 `/api/chapters/{id}/move` 相邻排序端点；一次拖动调整一个相邻位置，保存期间显示状态，失败后刷新服务端顺序。跨多位连续调用在文件/PostgreSQL 后端的 ID 语义不一致，因此不在前端扩张该合同。协作运行时后端明确禁用该端点，因此协作模式不展示拖拽。端点本身没有 expected-version 参数；若返回 409，沿用现有冲突文案并刷新回滚，不伪造版本控制。

验证结果：前端 47 files / 172 tests，token lint 与 production build 通过；Impeccable detector 对新增规划文件无命中，仅报告 `novel.css` 中 4 条既有 side-tab 警告。后端大纲/卷/场景与章节生命周期定向回归 8 passed / 1 skipped（real PostgreSQL 不可用）。

Phase 4 最终包：

- package/evidence root：`D:\小说\.desktop-v1-acceptance-p4-20260824-r15`
- ASCII launcher staging：`D:\AI-Novel-Studio-P4-Application-r15`
- verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`

下一阶段：Phase 5 图片模块 Visual Context 产品化。

## Phase 5 交付（代码、自动化与正式 DesktopHost）

实现文件：

- `frontend/src/novel/VisualContextPanel.tsx`
- `frontend/src/novel/VisualContextPanel.css`
- `frontend/src/novel/VisualContextPanel.test.tsx`
- `frontend/src/App.tsx`、`frontend/src/ui/ModuleWorkspaceRoutes.tsx`

Visual Context 复用现有小说、角色、地点、卷、场景、章节和资产 API。角色设定、年龄、身份、外貌、服装、当前地点、场景、当前卷、剧情位置及关联资产均只显示真实返回值；空值明确显示“未记录/暂无”。每项可在生成前单独勾选是否纳入提示词，面板只读，不修改 Canon。Provider 未配置时仍沿用现有图片生成失败状态。

验证结果：Visual Context 定向测试 2 passed，模块路由回归通过；token lint、production build、Impeccable detector 通过。Phase 5 正式包：`D:\小说\.desktop-v1-acceptance-p5-20260824-r16`，ASCII staging：`D:\AI-Novel-Studio-P5-Application-r16`。Verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`。

下一阶段：Phase 6 非小说模块状态中心。

## Phase 6 交付（代码、自动化与正式 DesktopHost）

实现文件：

- `frontend/src/ui/CapabilityStatusCenter.tsx`
- `frontend/src/ui/CapabilityStatusCenter.css`
- `frontend/src/ui/CapabilityStatusCenter.test.tsx`
- `frontend/src/ui/ModuleWorkspaceRoutes.tsx`

状态中心复用既有 `/api/multimodal/health`、`/api/video-providers`、`/api/plugins/runtime-status` 与 Workflow 查询，覆盖图片、视频、声音、资产、插件和 Workflow。状态明确区分已连接、已配置、可使用、未配置、未实现和 Feature Flag 关闭；接口存在但尚未具备生产能力的项目不会显示为可用。状态中心提供刷新、加载和失败状态。

验证结果：状态中心与模块路由测试通过；token lint、production build、Impeccable detector 通过。Phase 6 正式包：`D:\小说\.desktop-v1-acceptance-p6-20260824-r17`，ASCII staging：`D:\AI-Novel-Studio-P6-Application-r17`。Verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`。

下一阶段：Phase 7 Agent Activity Center。

## Phase 7 交付（代码、自动化与正式 DesktopHost）

实现文件：

- `frontend/src/novel/AgentActivityCenter.tsx`
- `frontend/src/novel/AgentActivityCenter.css`
- `frontend/src/novel/AgentActivityCenter.test.tsx`
- `frontend/src/App.tsx`

Activity Center 复用现有 `GET /api/agent-jobs`，展示 Agent 名称、真实 Job 状态、任务来源、输入摘要、输出摘要、开始/完成时间、错误信息、错误代码和重试关系。确定性 Agent 明确标记为规则校验；没有任务时不生成活动记录，读取失败保留真实错误状态。既有 AgentJobHistory、AgentTeamPanel 和审核/应用流程未替换。

验证结果：Agent Activity Center 与既有 Agent Team 测试 3 passed；token lint、production build、Impeccable detector 通过。Phase 7 正式包：`D:\小说\.desktop-v1-acceptance-p7-20260824-r18`，ASCII staging：`D:\AI-Novel-Studio-P7-Application-r18`。Verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`。

下一阶段：V0.6 收尾与正式 DesktopHost 全量验收矩阵。

## V0.6 收尾验收（2026-08-24）

- 前端全量：50 个测试文件 / 176 项测试通过；`npm run lint` 与 `npm run build` 通过。
- 后端定向：11 passed / 1 skipped；跳过项依赖当前环境未启动的真实 PostgreSQL，不把浏览器开发页或模拟数据当作通过。
- 正式 DesktopHost：`D:\小说\.desktop-v1-acceptance-p7-20260824-r18` / `D:\AI-Novel-Studio-P7-Application-r18`。最终 verifier：`application_ready=true`、`frontend=true`、`desktop_host_started=true`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`。
- Impeccable detector：Phase 5、Phase 6、Phase 7 新增界面均无新增命中；共享历史 `novel.css` 中既有 4 条 side-tab warning 保留，不在收尾阶段扩大修改范围。

V0.6 当前交付闭环已完成。后续 Phase 8+ 保持 Deferred，待有 PostgreSQL 的 CI/验收机补跑受阻 E2E 与全量后端回归后再扩展底层能力。

## 主控 AI 与多模态运行时（增量交付）

- 新增 AI 控制中心：集中管理 DeepSeek 主控/文本、DDSHub 图片、OpenAI 视觉/配音及自定义图片/配音/视频 Provider 的凭据、模型与健康状态。
- 新增只读主控问答：`POST /api/agent/chat` 复用既有 Generation Runtime，默认禁止写入、删除、发布和外部操作；用户偏好只有在明确授权后才会注入。
- 新增独立用户偏好记忆：本地 `user_preferences.json`，与小说 Lore、Canon、角色记忆分离，支持保存开关、主控读取开关和 Harness 授权开关。
- DeepSeek Harness 作为可选本地运行时接入：不自动下载或启动，仅允许本机地址；启动前执行授权、端口和版本兼容性检查；服务只管理自身启动的进程，退出时自动清理。
- 控制中心显示 Harness 运行状态、PID、版本兼容性及最近启动/停止动作；未授权或版本不兼容时启动按钮保持禁用。

验证：前端 `npm run build`、`npm run lint` 通过；Harness 进程服务、API 与关闭清理测试 4 passed。Harness 仍是可选运行时，不宣称已具备完整软件操作 Agent 能力。

## Harness 只读上下文与审计（增量交付）

- 新增 `GET /api/harness/context-contract`，明确 Harness 可读取的工作区摘要、章节、Lore、角色、地点和模型状态范围。
- 新增 `GET /api/harness/context`，向已授权的本地 Harness 提供真实 Agent Context；默认 local、只读，不暴露独立 instruction 字段。
- 新增访问审计：记录时间、小说、章节、Agent、读取范围和结果，不记录正文、Prompt、密钥或模型响应；读取失败也会留下元数据事件。
- 审计支持按小说、Agent 和结果筛选，支持 CSV 导出；清理必须显式 `confirm=true`，控制中心使用二次确认和危险操作样式。

验证：Harness 上下文、合同、审计与清理测试通过；前端审计 API 回归 9 passed，production build 与 UI token lint 通过。当前仍未开放任何写入、删除、生成提交、发布或外部请求工具。

## 多模态创作工作区（增量交付）

- 图片工作区新增无限画布式参考区：支持从资产库选择图片、粘贴参考地址、拖拽节点、删除节点和语义标签。
- 视频工作区新增导演台：支持镜头名称、时长、机位运动、动作描述和新增镜头；提供白膜、角色、场景、动作参考入口。
- 参考语义统一为结构化约束：图片支持角色、场景、服装、构图；视频支持白膜、角色、场景、动作。
- 图片生成 API 支持可选 `constraints` 元数据；视频帧配置 API 支持可选 `constraints` 字段，保持现有起始帧/结束帧兼容。
- 约束变化可通过 `onConstraintsChange` 输出标准对象，后续可直接接入 Provider 任务提交；当前界面不会伪造生成结果。

验证：多模态导演台前端测试 2 passed；production build 与 UI token lint 通过。实际图片、视频生成能力仍以已配置 Provider 健康状态为准。

补充：现有视频轨道由剧本镜头、转场清单和 Motion Task 共同组成；AI 转场建议、Transition Prompt、Motion Prompt、转场批准、任务执行、远端同步和资产导入均已接入。新增 `saveDirectorShotConstraints` 适配方法，可按 `shot_id` 将导演台的镜头、白膜和参考素材约束写入现有 Motion Task 帧配置。前端 API 回归达到 10 passed；尚未自动执行视频任务。

## 多模态一致性与声音编排（增量交付）

- 一致性锁定支持角色外观、服装、场景结构、光线、时间/天气和关键道具；档案可本地保存、切换、锁定、导入、导出和删除。
- 关键道具可从小说资产库选择图片并保存资产 ID；约束包包含 `prop_asset_ids`，不复制或修改资产库文件。
- 导演台镜头支持对白、环境音、动作音效、音乐入点、字幕、声音档案和情绪字段；`synthesizeDirectorShotDialogue` 可将镜头对白适配到现有语音合成 API。
- 新增 `DirectorShotCard` 与 `DirectorShotList`，支持配音生成、音频预览、响应式布局和共享镜头状态；高级卡片视图可按小说本地记忆。
- 约束包新增一致性检查结果和编辑视图标识，允许外部任务区分通过/警告以及无限画布、旧镜头或高级卡片来源。

验证：多模态导演台测试 6 passed，DirectorShotCard 测试 3 passed，DirectorShotList 测试 1 passed；production build 与 UI token lint 通过。配音与视频任务仍需用户明确触发，Provider 不可用时显示真实失败状态。

新增本地持久化基础：`useMultimodalWorkspacePersistence` 已抽出工作区参考节点、导演台镜头、快照创建/恢复/清理和最近 5 条快照的独立 Hook，并有 1 项 Hook 回归测试。当前导演台仍使用兼容中的内置状态逻辑，Hook 迁移尚未替换生产路径。

持久化迁移进展：导演台镜头和参考节点已接入统一 Hook；清理前创建带备注的单条快照，历史列表支持按时间和备注恢复，最多保留 5 条。Hook 测试 3 passed，导演台测试 7 passed，production build 通过。高级卡片和一致性档案仍保持独立本地存储边界。

约束包迁移：工作区支持 JSON 导入预览、用户确认、模式不匹配警告和结构校验；Hook 支持导入参考节点与镜头，拒绝无效结构。Hook 导入/快照测试 5 passed，导演台交互测试 7 passed。当前导入不会自动恢复 Provider 凭据或远端任务状态，只恢复本地编排数据。

绑定清单与持久化闭环：参考节点和镜头已完全统一由 `useMultimodalWorkspacePersistence` 管理，避免组件状态与本地存储双数据源；角色/场景/服装参考会自动进入绑定清单，道具图片勾选会自动添加，取消勾选会移除对应自动绑定。绑定清单随约束包导出，确认导入时恢复角色、场景和道具索引；手工移除仍可在清单面板中完成。

验证：相关导演台、持久化、绑定清单和清单面板测试共 16 passed；`npm run lint` 与 `npm run build` 通过。

导入集成回归：新增约束包确认导入和无效绑定保护测试，验证导入后参考节点与角色/场景/道具清单均恢复，格式不合法时现有工作区不被覆盖。导演台测试现为 10 passed。

导入安全提示：约束包预览现在显示镜头约束状态统计；包含失败镜头时显示风险提示；图片/视频模式不匹配时要求二次确认。导入预览已抽为独立组件，避免继续维护超长内联 JSX。

批次编排基础：导演台支持已确认镜头的 JSON/CSV 批次清单导出、最近 5 个本地批次草稿版本、按版本恢复、单个删除和全部清空确认。批次检查不会自动提交 Provider 任务，仍需后续接入真实批量 Motion Task。

并行开发首轮：新增 `MotionTaskBatchControls`，支持待执行 Motion Task 批量提交、状态汇总和部分失败反馈；图片生成面板加入 QUEUED/RUNNING/SUCCEEDED/FAILED 本地任务生命周期，仅在真实 API 返回资产地址后展示结果；导演台配音工具支持失败配音批量重试。相关模块均保留真实 Provider/API 状态，不生成占位结果。

高级镜头卡片字段补齐：卡片编辑器现覆盖环境音、动作音效和音乐情绪/入点，与旧内联镜头字段保持一致；对白、字幕、声音档案、情绪及配音生成能力继续保留。相关镜头组件测试 15 passed，lint/build 通过。

视频时间线增量：`VideoTimeline` 已正式挂载导演台，支持镜头排序、时长编辑、拆分、相邻合并、CUT/DISSOLVE/WIPE/FADE 转场选择及转场时长编辑。时间线内部明确区分镜头时长、转场时长和片段总占用时长，后续可据此扩展字幕轨、音频轨和 EDL 导出；当前仍只修改本地编排数据，不伪造视频生成结果。时间线与导演列表回归 9 passed，lint/build 通过。

时间线多轨增量：新增字幕/对白轨与音频轨可视化，分别读取 `subtitle`/`dialogue` 以及 `voice`、`ambience`、`sound_effect`、`music` 字段；空字段显示未设置状态，不创建虚假媒体。新增 `buildTimelineTracks` 为后续字幕导出、音频混音和任务提交提供统一区间数据。时间线测试 6 passed，lint/build 通过。

时间线导出增量：新增 JSON 编排导出与 CMX 风格 EDL 导出按钮，包含镜头区间、镜头/转场时长、转场类型、字幕和音频元数据；不写入密钥、Provider 响应或虚构媒体地址。时间线测试 7 passed，lint/build 通过。

字幕编辑增量：时间线支持直接编辑每个镜头的字幕/对白，并通过导演台 `onChange` 回写镜头编排数据；导出 JSON/EDL 会使用最新文本。时间线测试 8 passed，lint/build 通过。

关键帧编排增量：镜头新增 `keyframes` 秒数列表，可在时间线直接编辑并显示数量，JSON 导出保留关键帧标记，供后续 Provider 任务或关键帧预览使用；当前不生成或伪造帧文件。时间线测试 9 passed，lint/build 通过。

关键帧校验增量：新增 `parseKeyframeMarkers`，对关键帧进行数字、非负、镜头时长范围校验，并自动去重排序；无效标记不会写入镜头。时间线测试 9 passed，lint/build 通过。

关键帧可视化增量：视频轨片段会按镜头内相对时间显示关键帧标记，并提供可访问标签；越界数据不会渲染。时间线测试 10 passed，lint/build 通过。

桌面端 UI 重构第一轮：统一 AppShell 的冷灰工作区、青绿色动作色、顶部模块活动态、三栏边界和底部运行状态 Dock；新增项目级 `docs/DESIGN.md` 固化信息架构、交互、状态和未来 Copilot/Provider/媒体预览插槽。多模态导演台进一步迁移为分区制作工作区，参考画布、镜头区、约束区和高级镜头卡片使用统一面板层级。AppShell/FeatureLauncher 8 passed，多模态导演台相关测试 14 passed，lint/build 通过。

时间线 UI 迁移：视频时间线新增独立样式层，统一视频、字幕、音频轨的分组、边界、导出操作和响应式布局；关键帧、转场状态、字幕编辑、JSON/EDL 导出逻辑保持不变。时间线与导演列表测试 15 passed，lint/build 通过。

模块路由壳层迁移：通用图片、视频、声音、工作流、插件和资产模块不再使用空侧栏与空 Inspector，新增工作区导航、最近编辑/待处理任务入口，以及 AI Copilot、Provider 状态、审批队列和媒体预览预留槽。现有模块渲染和回调保持不变，路由/AppShell 回归通过。

Inspector 上下文增量：通用模块 Inspector 现在显示当前小说项目、工作区类型和真实任务等待状态，并按模块保留不同的关注领域与后续接入槽；没有 Provider 结果时明确显示等待状态，不伪造运行完成。ModuleWorkspaceRoutes 测试、lint/build 通过。

工作台首页与控制中心迁移：入口体验新增创作空间概览、小说数量、最近小说、故事线/分支信息和真实任务空态；保留新建、导入、打开小说和空间切换回调。插件模块同时挂载 AI 控制中心与插件管理，Provider、Harness、主控问答、用户记忆和审计能力进入统一工作区。EntryExperience/ModuleWorkspaceRoutes 测试 9 passed，lint/build 通过。

写作 Inspector 迁移：小说模式右侧检查面板新增当前章节上下文（标题、章节号、版本）、写作目标和 AI Writing Panel 分区；保留变体生成、草稿审核、冲突处理、模型选择、凭据和运行诊断回调。App/AiWritingPanel 回归 24 passed，lint/build 通过。

小说工作区视觉迁移：左侧章节树新增“章节导航 / 创作工具”层级，中央编辑器增加当前章节身份栏并统一编辑区高度和滚动边界；ChapterTree、FeatureLauncher、编辑器和保存逻辑保持不变。相关回归 21 passed，lint/build 通过。

状态 Dock 首次接入真实 Job：小说模式底部状态栏同时显示保存状态、连接模式和当前 AI 生成 Job 的生成中/完成/失败/取消/无运行任务状态；不会将未创建的图片、配音或视频任务显示为运行中。App 回归 12 passed，lint/build 通过。

跨模块任务 Dock 增量：图片生成面板、导演台配音批次和 Motion Task 批量控制均发布统一 `studio:task-summary` 事件，AppShell 合并显示运行、失败和排队数量。各模块继续使用真实任务状态，未创建任务发布空摘要。图片/配音/Motion 回归通过，lint/build 通过。

失败任务入口增量：底部 Dock 在存在真实失败任务时显示“查看失败任务”操作，点击后打开 Command Palette 并预填失败任务筛选；无失败任务时不显示该入口。AppShell 回归、lint/build 通过。

失败任务摘要 Inspector：底部 Dock 的失败入口现在打开统一任务摘要面板，按图片、配音、Motion 来源列出真实失败数量，并明确当前仅为摘要级信息；详细错误和重试仍由对应模块提供。AppShell 回归、lint/build 通过。

失败来源定位增量：摘要面板中的图片、配音、Motion 失败来源变为可操作行，点击会切换到对应工作区并关闭面板；重试仍由各模块真实控制。AppShell 回归、lint/build 通过。

模块失败聚焦闭环：配音、图片和 Motion Task 模块均监听 `studio:focus-failed-tasks`；配音定位批量重试按钮，图片定位重新生成按钮，Motion 定位批量控制区并显示检查提示。所有操作继续使用各模块真实重试语义。相关回归、lint/build 通过。

桌面视觉验收增量：Playwright 视觉基线的小说、图片、视频工作区以及 1366×768、1440×900、1920×1080 壳层几何检查通过。生产 NOVEL 路由和冲突页仍因本地 API 连接拒绝未完成真实数据验收。Impeccable detector 仅报告无限画布网格（真实画布允许项）和主控回答侧边强调；后者已改为完整 token 边界与浅背景，lint/build 通过。

紧凑桌面验收增量：新增 1024×768 壳层几何和视口溢出检查，验证 Inspector 覆盖模式、200px 导航栏和无页面横向滚动；视觉套件新增用例已写入，生产路由数据验收仍受本地 API 未启动阻塞。

任务 Dock 行为回归：AppShell 新增失败摘要打开、来源行定位和模块切换测试，确保真实任务事件能够稳定触发任务面板与视频工作区跳转。AppShell 测试 4 passed，lint/build 通过。

失败任务元数据增量：统一摘要现在可携带失败任务 ID 与安全错误摘要，失败面板按来源显示真实失败任务信息；不包含 Prompt、密钥或完整 Provider 响应。taskSummary/AppShell 回归 8 passed，lint/build 通过。

失败任务精确定位增量：`studio:focus-failed-tasks` 事件新增可选 `taskId`，失败来源跳转会携带对应失败 ID，目标模块可据此选中具体任务或镜头；当前兼容模块级聚焦。taskSummary/AppShell 回归 8 passed，lint/build 通过。

配音精确聚焦增量：DirectorVoiceToolbar 消费事件中的 `taskId`，在当前批次匹配镜头/任务 ID 后执行精确失败聚焦；无匹配 ID 时回退到模块级重试按钮聚焦。配音回归、lint/build 通过。

图片精确聚焦增量：ImageGenerationPanel 消费事件中的 `taskId`，匹配当前图片任务后聚焦重新生成按钮；无任务 ID 时保持模块级聚焦兼容。图片回归、lint/build 通过。

Motion 精确聚焦增量：MotionTaskBatchControls 消费事件中的 `taskId`，匹配当前任务后显示具体任务 ID 并聚焦批量控制区；无匹配时回退模块级提示。Motion 回归、lint/build 通过。

失败任务面板键盘可达性：打开后焦点自动进入关闭按钮，Escape 只关闭失败任务对话框并阻止事件继续触发全局 Inspector 收起；新增焦点和关闭回归。AppShell 测试 5 passed，lint/build 通过。

失败列表键盘导航：失败任务行支持上下箭头循环移动焦点，Enter 继续使用按钮原生激活执行精确定位；新增多失败项焦点回归。AppShell 测试 6 passed，lint/build 通过。

失败对话框焦点闭环：Tab/Shift+Tab 在关闭按钮和失败任务行之间循环，Escape、关闭按钮和遮罩关闭后焦点返回底部任务 Dock 触发按钮，避免键盘焦点丢失。AppShell 测试 6 passed，lint/build 通过。

Command Palette 可访问性：命令列表使用 listbox/option 语义并同步 `aria-selected`；Tab/Shift+Tab 在搜索框和命令项间循环，Escape 或遮罩关闭后焦点返回顶部搜索入口。AppShell 回归、lint/build 通过。

Command Palette 真实操作接入：查看失败任务命令读取当前失败数量并打开真实摘要面板；Provider 控制中心命令切换到已挂载 AiControlCenter 的插件工作区，不再显示“待接入”。AppShell 测试 7 passed，lint/build 通过。

资产库 UI 迁移：新增资产数量与类型筛选工具栏、响应式媒体网格和稳定 3:2 预览区域；安全鉴权下载、懒加载、上传/删除错误逻辑保持不变。旧 3px 错误侧边线改为完整语义边界。AssetLibraryPanel 回归、lint/build 通过。

转场状态增量：新增 `getTransitionStatus`，将转场显示为直接剪切、参数不完整、已配置参数或等待 Provider 任务；状态仅依据本地镜头字段，不伪造生成完成。时间线测试 11 passed，lint/build 通过。

工作流任务 Dock 接入：`WorkflowPanel` 将已加载的真实运行记录、`AgentQueuePanel` 将真实队列记录发布到统一任务摘要；失败项仅携带运行/节点 ID 和安全错误摘要。Dock 可将 workflow/agent 来源切换到工作流模块，并在目标挂载后的下一帧精确聚焦运行区或 Agent 任务行；首次进入尚无运行记录时回退聚焦刷新操作。两个面板同时补充加载失败与重试反馈，不伪造运行状态。

验证：Workflow/Agent/AppShell/taskSummary 定向回归 4 files/13 tests、UI token lint、production build（1831 modules）和 Impeccable detector 通过。

插件治理 UI 迁移：插件管理从线性文本列表重构为桌面治理工作区，新增本地发现/注册/清单激活/代码执行摘要、manifest 搜索、验证错误、Provider 与运行时边界、权限审核、清单激活和停用状态。界面明确 V1 激活只作用于 manifest，插件代码执行仍受 `execution_supported`、沙箱和隔离策略约束；注册不会自动授权，授权与激活均保留真实 API 调用及加载/成功/失败反馈。

验证：PluginManagerPanel/ModuleWorkspaceRoutes 定向回归 2 files/3 tests、UI token lint、production build（1832 modules）和 Impeccable detector 通过。

资产 Inspector 闭环：资产网格新增可访问的选择按钮、持久选中视觉状态和删除已选资产后的状态清理；新增 `AssetInspector`，通过现有鉴权下载 API 显示真实图片、视频或音频 Blob 预览，并展示大小、资产 ID、SHA-256、更新时间和原始文件下载。通用模块路由与生产 `App.tsx` 资产路由均已接入同一选择/Inspector 组合；未选择、不可预览和下载失败时显示真实状态，不推断尚未记录的引用关系。

验证：AssetLibraryPanel/AssetInspector/ModuleWorkspaceRoutes 定向回归 3 files/5 tests、UI token lint、production build（1835 modules）和 Impeccable detector 通过。

工作流 Inspector 闭环：新增真实运行/Agent 节点检查器，工作流运行行和 Agent 队列行均可将运行 ID、状态、当前节点、Agent 角色与安全错误摘要送入右侧 Inspector。Dock 精确定位会同步更新 Inspector；目标模块首次挂载、详细记录尚未读取时明确显示“待刷新”，不伪造节点详情。通用模块路由与生产 `App.tsx` 工作流路由已统一接入，工作流输入、Agent 输出和完整响应不进入检查器状态。

验证：WorkflowPanel/AgentQueuePanel/WorkflowInspector/ModuleWorkspaceRoutes/AppShell 定向回归 5 files/12 tests、UI token lint、production build（1838 modules）和 Impeccable detector 通过。

图片任务 Inspector 闭环：图片生成面板向工作区发布最小真实任务合同，包括固定任务 ID、DDSHub Provider、`gpt-image-2` 模型、生命周期、真实 `asset_uri`、导入状态和安全错误摘要；Prompt、凭据和完整 Provider 响应不进入 Inspector 或 Dock 元数据。右侧检查器显示真实生成结果、排队/运行/成功/失败状态及资产库状态；Provider 未返回 URI 或请求失败时保持无预览空态，不创建占位结果。历史记录仅在用户明确点击预览后更新检查器。

验证：ImageGenerationPanel/ImageTaskInspector/ModuleWorkspaceRoutes/AppShell/taskSummary 定向回归 5 files/16 tests、UI token lint、production build（1840 modules）和 Impeccable detector 通过。

视频任务 Inspector 闭环：`ScreenplayPanel` 的 Motion Task 行新增检查入口，并将任务 ID、剧本/转场 ID、Provider、模型、进度、首尾帧、真实结果 URL、资产 ID 和安全失败摘要映射到右侧 Inspector。运行任务显示真实进度，视频仅在结果包含 URL 时提供点击播放且不自动播放；本地镜头、首尾帧或转场配置不再被误示为视频已生成。Dock 的 Motion 失败定位会同步选择任务，跨模块首次进入尚未加载记录时显示待刷新状态。Prompt 与完整回调负载不进入检查器合同。

验证：VideoTaskInspector/MotionTaskBatchControls/ScreenplayPanel motion binding/ModuleWorkspaceRoutes/AppShell 定向回归 5 files/14 tests、UI token lint、production build（1842 modules）和 Impeccable detector 通过。

声音任务 Inspector 闭环：单次配音发布独立 `speech` 任务来源，有声书章节队列发布独立 `audiobook` 来源，避免与导演台 `audio` 批次互相覆盖 Dock 摘要。右侧 Inspector 展示真实 Provider、模型、音色、情绪、章节 ID、生命周期、导入状态和音频 URI；只有 API 返回真实 URI 时提供音频播放。失败、待刷新和无结果状态均明确显示，不写入朗读全文、凭据或完整 Provider 响应。Dock 的 `speech`/`audiobook` 失败来源均切换到声音工作区。

验证：SpeechSynthesisPanel/AudiobookManifestPanel/AudioTaskInspector/AppShell/taskSummary/ModuleWorkspaceRoutes 定向回归 6 files/17 tests、UI token lint、production build（1844 modules）和 Impeccable detector 通过。

插件 Inspector 闭环：插件治理的已注册列表新增可访问选择入口，右侧 Inspector 显示 manifest 能力、请求权限、已授予权限、清单状态、沙箱和隔离策略。执行边界明确显示当前代码执行是否允许；V1 `execution_supported=false` 时不会把清单激活误报为插件代码已运行。Inspector 不接收密钥、插件代码或运行时完整响应；无选择时保持真实空态。

验证：PluginManagerPanel/PluginInspector/ModuleWorkspaceRoutes 定向回归 3 files/5 tests、UI token lint、production build（1846 modules）和 Impeccable detector 通过。

统一任务 Dock 路由验收：补充 `speech`、`audiobook`、`audio`、`image`、`motion`、`workflow`、`agent` 七类失败来源矩阵，确认 Dock 均切换到正确工作区；声音两个新增来源不会回退到视频，工作流/Agent 不会误切图片或声音。插件治理与 Inspector 选择合同同步回归。

验证：AppShell/PluginManagerPanel/PluginInspector 定向回归 3 files/18 tests、UI token lint、production build（1846 modules）和 Impeccable detector 通过。

全量前端验收批次：完整 Vitest 回归通过 74 files/261 tests，UI token lint 和 production build（1846 modules）通过。视觉套件中小说、图片、视频基线以及 1366×768、1440×900、1920×1080 和紧凑视口溢出检查通过；1024×768 几何、生产 NOVEL 路由和冲突比较页因本地 API/数据库未启动出现阻塞或不稳定，未将浏览器开发页结果当作 DesktopHost 验收证据。测试结束后已清理残留 Playwright/Vite 进程。

视觉断言修正：1024×768 下 AppShell 按响应式契约默认收起 Inspector，几何用例原先错误断言 340px；现改为验证紧凑桌面 Inspector 宽度为 0，同时保留覆盖层与无横向溢出检查。重跑小说/图片/视频基线、1024/1366/1440/1920 几何和紧凑溢出共 8 tests 全部通过。生产 NOVEL 与冲突页仍等待本地 API/PostgreSQL。

正式 DesktopHost 验收包（2026-08-26）：从当前前端、后端、数据库迁移和 DesktopHost 源码重新装配 `0.7.0 Beta` Windows 验收包，仅复用上一次冻结的 Python/PostgreSQL 基础运行时。包与证据根目录为 `D:\小说\.desktop-v1-acceptance-p8-20260826-r19`，DesktopHost 同轮发布 DLL SHA-256 为 `07069966D8B3064DC984BDD53FF1D660B4A75CE3800752044463F0519A130873`，已生成来源、发布、应用和包清单及 ZIP。

包内运行时验证通过：`ready=true`、`packaged=true`、`frontend_ready=true`、PostgreSQL 16 中文往返正确、用户数据保留、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`。正式 DesktopHost 验证同样通过：`application_ready=true`、`desktop_host_started=true`、`frontend=true`、数据库与后端均仅监听 `127.0.0.1`、`public_listeners=0`、`secret_exposure=0`、`exit_code=0`。本轮结果是 packaged DesktopHost 实际启动证据，不是 Vite/浏览器开发页替代证据。

DesktopHost 视觉证据增量：新增 `scripts/capture_packaged_desktop_evidence_v070.py`，通过实际 DesktopHost PID 定位 Win32 窗口，使用 Windows 原生屏幕捕获生成 PNG 和不含凭据的 JSON 几何清单。证据保存于 `D:\小说\.desktop-v1-acceptance-p8-20260826-r19\visual-evidence-r2`；1024×720、1440×900、1920×1080 三档实际窗口尺寸与请求完全一致，均为 120 DPI。像素检查确认 WebView2 非黑屏、中文字体正常、首次启动的个人/团队入口在紧凑桌面下无文本重叠或横向裁切；同轮仍为 `application_ready=true`、`desktop_host_started=true`、`frontend=true`、`public_listeners=0`、`secret_exposure=0`。隔离用户目录中没有伪造小说或 Provider 数据，因此证据如实显示首次启动入口。
