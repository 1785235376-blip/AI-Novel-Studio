# Layout Rules

Use one AppShell with GlobalHeader, fixed ModuleSwitcher, ContextBar, LeftSidebar, MainWorkspace, RightInspector and StatusBar. NOVEL, IMAGE and VIDEO supply content through the ModuleWorkspace contract. Use the layout tokens in `frontend/src/ui/tokens.css`; do not create module-specific shells.

